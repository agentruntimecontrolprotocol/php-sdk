# email-vendor-leases

A triage agent receives leases for `email.search` and `email.read` but
not `email.send`. The denied send becomes structured feedback to the
model while a vendor event carries parsed metadata for dashboards.
