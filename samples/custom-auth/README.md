# custom-auth

Demonstrates a tiny bearer-token verifier shape. Real deployments wire
the verifier into `AuthRouter` or a runtime-specific auth adapter.

Run:

```sh
php samples/custom-auth/main.php demo-token
```
