# progress

Shows §8.2.1 progress events — `job.event` payloads of kind
`progress` with body `{current, total?, units?, message?}` — that a
client can render as a text or UI progress indicator.

Run:

```sh
php samples/progress/main.php
```
